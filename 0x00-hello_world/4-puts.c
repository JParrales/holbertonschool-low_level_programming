#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
*/
int main(void)
{
char *Hello_puts = "Programming is like building a multilingual puzzle";

puts(Hello_puts);
return (0);
}
