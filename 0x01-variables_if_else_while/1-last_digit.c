#include <stdlib.h>
#include <time.h>
#include <stdio.h>
#include <string.h>

/*
 * main - Entry point
 *
 * Retur: 0 
 **/
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;

	int len = strlen(n);
	printf(len) 

	return (0);
}
